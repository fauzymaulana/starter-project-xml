<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CapStoneExpert" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CapStoneExpert" parent="Base.Theme.CapStoneExpert" />
    <style name="DialogOverlayTheme" parent="Theme.AppCompat.Dialog.Alert">
        <!--        <item name="android:windowBackground">@android:color/transparent</item>-->
        <item name="android:windowMinWidthMinor">0%</item>
    </style>

    <style name="ThemeCardView">
        <item name="cardBackgroundColor">@color/background_item_light</item>
    </style>

    <style name="FontColor">
    </style>

    <style name="FontColor.Title">
        <item name="android:textColor">@color/label_light</item>
    </style>
    
    <style name="BColor">
        <item name="backgroundTint">@color/background_bottom_nav</item>
        <item name="background">@color/background_bottom_nav</item>
        <item name="android:background">@color/background_bottom_nav</item>
    </style>
</resources>